# Environment variables for Team Workspace.
# Copy this file to `.env` and fill in values as features are added.
# All Vite-exposed variables must be prefixed with VITE_.
# Any code that reads these must validate them at the boundary (they are external input).
#
# Example (uncomment and set when needed):
# VITE_API_BASE_URL=
