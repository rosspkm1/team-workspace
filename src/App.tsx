import { appName } from '@utils/appName';

// Minimal application shell. Routing, styling systems, and features are added
// by later stories in the Foundation epic.
function App() {
  return <h1>{appName}</h1>;
}

export default App;
